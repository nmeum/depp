<section id="blob">
	{{ template "breadcrumb.tmpl" .CurrentFile }}

	<pre class="blob">
{{- if .CurrentFile.IsSubmodule -}}
	{{- $module := (.GetSubmodule .CurrentFile) -}}
	{{ $module.Url }} @ {{ $module.IndexId }}
{{- else -}}
	{{- $blob := .GetBlob (.CurrentFile) -}}

	{{- $lines := (getLines $blob) -}}
	{{- range $i, $line := $lines }}
<a id="L{{ $i }}" href="#L{{ $i }}">{{ getPadding (len $lines) $i }}{{ $i }}</a>{{ . }}
	{{- end }}
{{- end -}}
	</pre>
</section>
